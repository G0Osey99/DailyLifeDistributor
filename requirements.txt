flask>=3.0.0,<4
python-dotenv>=1.0.0,<2
pyyaml>=6.0,<7
openpyxl>=3.1.0,<4
google-auth>=2.28.0,<3
google-auth-oauthlib>=1.2.0,<2
google-api-python-client>=2.120.0,<3
# 2.32.0 fixed CVE-2024-35195 (a Session kept verify=False across requests
# after the first unverified call); 2.32.2 drops the 2.32.0/2.32.1 regressions.
requests>=2.32.2,<3
# 46.0.7 fixes PYSEC-2026-35, PYSEC-2026-36, CVE-2026-26007.
# Earlier 43.0.1 was already past the OpenSSL 3.3.2 line (CVE-2024-6119).
# Keep an upper bound so a major bump is a deliberate change.
cryptography>=46.0.7,<47
playwright>=1.40.0,<2
# Server-side WebSocket for the hybrid-agent relay (pulls in simple-websocket).
flask-sock>=0.7,<1
# Rate limiting on pairing + agent endpoints (Phase 3 hardening).
# In-memory storage is fine for the single-instance VPS; switch to a Redis
# URI (RATELIMIT_STORAGE_URI=redis://...) when going multi-instance.
flask-limiter>=3.5,<5
# Multi-tenant phase α: password hashing, 2FA (forward-declared for PR-γ),
# transactional email, signed tokens, CSRF.
argon2-cffi>=23,<24
pyotp>=2.9,<3
qrcode[pil]>=7.4,<8
resend>=0.7,<3
itsdangerous>=2.1,<3
flask-wtf>=1.2,<2
# Phase γ: 2FA + audit log.
# bcrypt for backup recovery codes + email-2FA codes (cost-resistant hashing).
# APScheduler for the nightly audit_log archive job.
bcrypt>=4.1,<5
apscheduler>=3.10,<4
